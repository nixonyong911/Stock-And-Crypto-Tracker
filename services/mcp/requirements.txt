# MCP Analysis Server Dependencies
fastmcp>=2.0.0,<3
asyncpg>=0.29.0
pydantic>=2.0.0
py-key-value-aio[redis]>=0.3.0
# Required for per-tier ASGI mounting (transitive deps of fastmcp,
# listed explicitly for clarity)
uvicorn>=0.27.0
starlette>=0.36.0
