use ai cli, instead of ai api, to save cost
metric
ai api 
centralized worker to handle cronjob, each worker's configuration, such as when to schedule, disable or enable worker, worker log, worker status, etc etc