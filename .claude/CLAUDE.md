# Stock and Crypto Tracker

Project instructions are loaded automatically from `.claude/rules/`.

All rule content lives in `instruction/rules/` (source of truth).
All skill content lives in `instruction/skills/` (on-demand procedures).
